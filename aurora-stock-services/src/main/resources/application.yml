# 服务端口
server:
  port: 10010
# 服务名称
spring:
  application:
    name: aurora-stock-services
  # Nacos
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        username: nacos
        password: nacos
        namespace: public
